name: Publish Docker Images

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "Image tag to publish (example: v1.1.0). Leave empty to use current git tag or sha."
        required: false
        type: string

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      REGISTRY: ghcr.io
      OWNER: ${{ github.repository_owner }}
      REPO: ${{ github.event.repository.name }}
      VERSION: ${{ github.event_name == 'release' && github.event.release.tag_name || inputs.version || github.ref_name || github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Normalize image namespace
        run: |
          echo "OWNER_LC=${OWNER,,}" >> "$GITHUB_ENV"
          echo "REPO_LC=${REPO,,}" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push CPU image
        run: |
          docker buildx bake cpu \
            --set cpu.platform=linux/amd64,linux/arm64 \
            --set cpu.tags[0]=${REGISTRY}/${OWNER_LC}/${REPO_LC}-cpu:${VERSION} \
            --set cpu.tags[1]=${REGISTRY}/${OWNER_LC}/${REPO_LC}-cpu:latest \
            --push

      - name: Build and push GPU image
        run: |
          docker buildx bake gpu \
            --set gpu.platform=linux/amd64 \
            --set gpu.tags[0]=${REGISTRY}/${OWNER_LC}/${REPO_LC}-gpu:${VERSION} \
            --set gpu.tags[1]=${REGISTRY}/${OWNER_LC}/${REPO_LC}-gpu:latest \
            --push
